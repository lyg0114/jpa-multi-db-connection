server:
  port: 7001

spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:waterleak
    username: sa
    password:
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect